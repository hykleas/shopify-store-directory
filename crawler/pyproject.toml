[tool.ruff]
line-length = 100
target-version = "py312"
exclude = [".venv", "__pycache__", ".model_cache"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "ASYNC", "C4", "SIM"]
ignore = ["E501", "B008", "SIM108"]

[tool.ruff.lint.per-file-ignores]
"categorize/taxonomy.py" = ["E501"]
